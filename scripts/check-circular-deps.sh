#!/bin/bash

echo "🔍 Проверка циклических зависимостей..."

# Запускаем madge для проверки циклических зависимостей
result=$(madge --circular --extensions ts,tsx,js,jsx src/ 2>&1)
exit_code=$?

if [ $exit_code -eq 0 ]; then
    echo "✅ Циклические зависимости не найдены!"
    exit 0
else
    echo "❌ Ошибка при проверке зависимостей:"
    echo "$result"
    exit 1
fi
